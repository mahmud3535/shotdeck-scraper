# shotdeck-scraper
This project is a Python-based ShotDeck scraper packaged as a GitHub-ready CLI tool. It logs in using credentials from a .env file, crawls ShotDeck search/results pages, extracts image links and metadata, and downloads the selected stills to a local folder.
