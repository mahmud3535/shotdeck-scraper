# Contributing

- Use `ruff format .` and `ruff check .` before opening a PR.
- Add tests for pure helper functions where possible.
- Avoid committing any credentials, `.env` files, or scraped outputs.
